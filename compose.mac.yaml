# Mac 에서 호스트의 mlx-lm 을 쓸 때. Docker Desktop 은 GPU 패스스루가 없어서
# 모델은 컨테이너 밖에 있어야 합니다.
#   mlx_lm.server --model mlx-community/... --port 8080
services:
  runtime:
    environment:
      LLM_BASE_URL: http://host.docker.internal:8080/v1
    extra_hosts: ["host.docker.internal:host-gateway"]
